{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window: core events/window/app APIs used by the splash page.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
